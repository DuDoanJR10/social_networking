import React from 'react';
import '../styles/Footer.scss';
import { Layout } from 'antd';

const { Footer } = Layout;

const FooterComponent = () => {
  return <Footer className="bg-orange-300 p-0">Footer</Footer>;
};

export default FooterComponent;
